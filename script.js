// WS Wattson — apenas menu mobile, scroll suave e entrada discreta dos blocos.
document.addEventListener('DOMContentLoaded',()=>{const botao=document.querySelector('.menu');const nav=document.querySelector('.navegacao');if(botao&&nav)botao.addEventListener('click',()=>nav.classList.toggle('aberto'));document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{const alvo=document.querySelector(link.getAttribute('href'));if(alvo){event.preventDefault();alvo.scrollIntoView({behavior:'smooth'});nav?.classList.remove('aberto')}}));const observador=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visivel');observador.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.entrada').forEach(item=>observador.observe(item));});
